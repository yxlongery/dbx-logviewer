---
id: "TD-20260925-001"
type: td
category: "task"
status: "open"
priority: "medium"
area: ["backend"]
aliases: ["SSH模式未实现"]
created: 2026-09-25
---

# SSH 模式未实现

- **来源**：首版上架会话 2026-09-25
- **问题/计划**：连接表单 SSH 字段已就绪，后端 `connection/connect` 遇到 `mode=ssh` 直接返回可行动错误。需引入 `russh`（纯 Rust，无系统依赖）实现 SSH 通道：connect 建会话、list 经 SFTP 读目录、search/tail 经 `tail -F` 或 SFTP seek 轮询
- **验证口径**：NAS DBX 上 `invoke connection/connect`（ssh 模式）成功 + `logs/list` 列出远端 `.log`
