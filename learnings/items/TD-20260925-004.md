---
id: "TD-20260925-004"
type: td
category: "task"
status: "done"
status-detail: "已完成（提交 e45d27a 联调、02017e6 升版）：查明 CI ubuntu 产物为 gnu 好包，musl 坏包是本地 Alpine 手动打的 dist 包误装生产所致，非商店链路问题，发 0.1.1 走正常流程即可"
priority: "high"
area: ["backend", "infra"]
aliases: ["商店0.1.0正式包musl坏、生产实际跑beta.2"]
created: 2026-09-25
---
# 商店 0.1.0 正式包 musl 坏、生产实际跑 beta.2

- **来源**：目录浏览改造联调（2026-09-25）
- **问题/计划**：`dist` 的 0.1.0 linux-x64 正式包内二进制是 musl（interpreter `/lib/ld-musl-x86_64.so.1`），在 Debian 系 DBX 容器里 `Failed to start plugin ... No such file or directory` 起不来；生产实际跑的是 0.1.0-beta.2（NAS 现编 gnu 版）。官方复用 workflow 产出的 5 平台包若全是 musl，商店正式版全坏。需查 workflow 构建矩阵，修成 gnu（或 musl-static），重发 0.1.1。
- **验证口径**：`file` 看落盘 bin interpreter 为 `/lib64/ld-linux-x86-64.so.2`，且 `POST /api/plugins/invoke {ping}` 通。
