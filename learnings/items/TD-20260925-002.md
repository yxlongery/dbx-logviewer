---
id: "TD-20260925-002"
type: td
category: "task"
status: "watching"
status-detail: "2026-09-25 后端全链路已过（gnu 包安装成功，link/list/search/异常/delete/tail+SSE 均符合预期），待浏览器实操与 playwright DOM 验证"
priority: "medium"
area: ["frontend", "backend"]
aliases: ["链接与删除功能 NAS 侧回验"]
created: 2026-09-25
---
# 链接与删除功能 NAS 侧回验

- **来源**：链接/删除功能会话 + 2026-09-25
- **问题/计划**：本地已验证（`cargo test` 7/7、`cargo build`、`node --check`、dev host 200 + 后端 ready），但 dev host 的 `/api/*` 直调被 403，且本容器无 playwright 工具，真实宿主交互尚未验证
- **可选解 / 计划内容**：NAS 上 `ssh` 进本机 `curl POST /api/plugins/invoke` 走 connect→link→list→search→tail→delete 全链路；浏览器打开工作台实操建链、`×` 删除、`confirm` 文案；playwright sidecar 做 DOM 级验证
- **验证口径**：invoke 全链路返回符合预期（含 `wasLink`、`target`）；UI 实操建链刷出 🔗 卡、删链留目标、删普通文件真删
