---
id: "TD-20260925-A01"
type: td
category: "task"
status: "open"
status-detail: "待 NAS 真机回验"
priority: "medium"
area: ["frontend"]
aliases: ["NAS真机回验实时监控timeoutMs修复"]
created: 2026-09-25
---
# NAS真机回验实时监控timeoutMs修复

- **来源**：本会话 + 2026-09-25
- **问题/计划**：本地仅做逻辑模拟与 JS 语法检查，真机工作台点“实时监控”是否收到 `streamId` 且无 `timeoutMs` 报错尚未验证
- **可选解 / 计划内容**：经 DBX 工作台打开本插件，选日志文件后点实时监控，观察状态切“实时监控中”且有增量推送；或 NAS 本机 `curl POST /api/plugins/invoke` 直调 `logs/tail`
- **验证口径**：工作台点实时监控无 `timeoutMs must be a number` 报错，且收到 `streamId` 与 `logs/append` 推送
